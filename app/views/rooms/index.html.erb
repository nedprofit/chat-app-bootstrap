<div class="container mt-3">
  <h2>Chat Rooms</h2>
  <div class="list-group">
    <% @rooms.each do |room| %>
      <a href="<%= room_path(room) %>" class="list-group-item list-group-item-action">
        <%= room.name %>
      </a>
    <% end %>
  </div>
  <%= link_to 'New Room', new_room_path, class: 'btn btn-primary mt-3' %>
</div>
